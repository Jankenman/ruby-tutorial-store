<h1><%= t ".title" %></h1>

<%= link_to "New product", new_product_path if authenticated? %>

<div id="products">
  <ul>
    <% @products.each do |product| %>
      <li class="product"><%= link_to product.name, product_path(product.id) %></li  class="product">
    <% end %>
  </ul>
</div>